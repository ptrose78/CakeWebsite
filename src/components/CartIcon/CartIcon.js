import { useSelector, useDispatch } from 'react-redux';
import './CartIcon.css'

const CartIcon = ( ) => {

  return (
    <div>
        <div className="cart-icon">
            🛒
        </div>
    </div>
  );
};

export default CartIcon;
